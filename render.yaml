# Render Blueprint — https://render.com/docs/blueprint-spec
# After deploy, set SMTP_* and JWT_SECRET_KEY in the Render dashboard (Environment).
services:
  - type: web
    name: ai-medical-assistant
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn --app-dir backend app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /healthz
    envVars:
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: DB_BACKEND
        value: sqlite
      - key: SQLITE_PATH
        value: /tmp/medical_assistant.db
      - key: USER_DATA_RETENTION_HOURS
        value: "24"
      - key: SHOW_OTP_IN_DEV
        value: "false"
      - key: SMTP_HOST
        value: smtp.gmail.com
      - key: SMTP_PORT
        value: "587"
      - key: SMTP_USE_TLS
        value: "true"
      # Set in Render dashboard (do not commit secrets):
      # SMTP_USER, SMTP_PASSWORD, SMTP_FROM (Gmail address + App Password)
      # OPENAI_API_KEY (optional)
